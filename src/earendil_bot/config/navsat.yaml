# NavSat Transform Node parameters
# Converts GPS (NavSatFix) to local odometry (/odometry/gps)
# Used by UKF Global for absolute positioning
#
# DATUM: Fixed reference point for the mission area.
#   Update lat/lon to match your real-world operating location.

navsat_transform_node:
  ros__parameters:
    use_sim_time: false
    frequency: 30.0
    delay: 0.0
    magnetic_declination_radians: 0.0   # TODO: Set for your location (e.g. Istanbul ≈ 0.087 rad)
    yaw_offset: 0.0                      # No correction needed for real hardware
    zero_altitude: true
    broadcast_cartesian_transform: false # UKF Global provides map→odom
    publish_filtered_gps: true
    use_odometry_yaw: false             # Use IMU yaw directly
    base_link_frame: base_footprint     # Prevents tf lookup failures
    wait_for_datum: true                # Force a deterministic world origin
    datum: [39.925050, 32.836956, 0.0]  # TODO: Update to real mission area origin

